/**
 * The character inventory, behind one door — the same shape as `panels/`.
 *
 * The route is a `createFileRoute` and a `<ShellGate>` around `InventoryPanel`;
 * everything the screen does lives here.
 */

export { InventoryPanel } from "./InventoryPanel";
